/* exported last */
// Define a new function 'last' with a parameter 'array'
// Assign a spot for the last 'element' from your 'array'
// Assign a counter taking the length of your 'array' and minus it by one
// Taking your 'array', access it with your counter and take that element and
// assign it to your spot
// Return that spot

function last(array) {
  var lastElement;
  var x = array.length - 1;
  lastElement = array[x];
  return lastElement;
}

// Define a new function 'last' with a single parameter 'array'
// Assign a spot to put your last element from your 'array'
// Assign a counter taking the length of your 'array' and subtract 1 from it
// Taking your 'array', access it using the counter and assign that to your last element
// Return your last element
