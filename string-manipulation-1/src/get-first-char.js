/* exported getFirstChar */
// define a function 'getFirstChar' with a single parameter 'string'
// define the variable 'firstChar'
// using the method 'charAt' and calling it at zero, assign the character to the
// variable 'firstChar'
// return the variable

function getFirstChar(string) {
  var firstChar;
  firstChar = string.charAt(0);
  return firstChar;
}

// Define a new function 'getFirstChar' with a single parameter of 'string'
// Create a place to assign where you store the first character of the string
// Acquire the first character through a method from your 'string'
// Assign and return that character
