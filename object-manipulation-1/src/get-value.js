/* exported getValue */
// Define a new function 'getValues' with tw parameters 'object' and 'key'
// Assign where you plan to return your values
// For every key in your object, push the value to where you had planned them
// return the values

function getValue(object, key) {
  var values;
  values = object[key];
  return values;
}

// Define a new function 'getValue' with two parameters 'object' and 'key'
// Assign where you plan to set your values
// For your 'object', access it with the 'key' and assign the value you get
// Return that value
