/* exported setValue */
// Define a new function 'setValue' with three parameters: 'object', 'key', and 'value'
// Take your property and assign it to the object accessed with the key

function setValue(object, key, value) {
  object[key] = value;
}

// Define a new function 'setValue' with three parameters: 'object', 'key', and 'value'
// Take your value and assign it to the object accessed by the key
